<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <header>
    <a class="logo-title" href="/">{{#if isAuthenticated}}{{user.name}}'s{{/if}}家庭記帳本</a>
    {{#if isAuthenticated}}
      <a class="btn-login" href="/users/logout">Logout</a>
    {{else}}
      <a class="btn-login" href="/users/login">Login</a>
    {{/if}}
  </header>
  
  <div class="container">
    {{{ body }}}
  </div>
  
  <script src="https://kit.fontawesome.com/a8874340d7.js" crossorigin="anonymous"></script>
</body>
</html>